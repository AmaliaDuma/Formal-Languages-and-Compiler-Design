{
	3a : int;   <- error identifier starts with 3
	b : int;
	c : int;
	maxNr : int;
	
	>>a;s
	>>b;
	>>c;
	
	if (a >= b && a >= c {   <- error missing )
		maxNr = a;
	} else if (b >= a && b >= c) {
		maxNr = b;
	} else {
		maxNr = c;
	}
	
	<<maxNr;
}